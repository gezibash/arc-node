version: v2
modules:
  - path: api/proto
lint:
  use:
    - STANDARD
  except:
    # Relay uses bidirectional streaming frames, not request/response
    - RPC_REQUEST_STANDARD_NAME
    - RPC_RESPONSE_STANDARD_NAME
breaking:
  use:
    - FILE
